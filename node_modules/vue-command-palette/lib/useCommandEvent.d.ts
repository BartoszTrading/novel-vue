import { Emitter } from 'mitt';
import type { ItemInfo } from './types';
declare type Events = {
    selectItem: ItemInfo;
    rerenderList: boolean;
};
declare const useCommandEvent: () => {
    emitter: Emitter<Events>;
};
export { useCommandEvent };
